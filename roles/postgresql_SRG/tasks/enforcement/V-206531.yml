############################################################
# PostgreSQL STIG | V-206531
# Ensure audit record includes source of event
############################################################

- name: Ensure log_line_prefix includes source identifiers
  become: true
  lineinfile:
    path: "{{ pg_conf_file }}"
    regexp: '^\s*#?\s*log_line_prefix\s*='
    line: "log_line_prefix = '%m %H %p %u %d %c %h %r %a %e '"
    backup: true
  notify: Restart PostgreSQL
  tags:
    - stig
    - V-206531
    - logging
