---
dependency:
  name: galaxy

driver:
  name: docker

platforms:
  - name: sonarqube-rocky9
    image: geerlingguy/docker-rockylinux9-ansible:latest
    pre_build_image: true
    privileged: true
    command: /lib/systemd/systemd
    capabilities:
      - SYS_ADMIN
    volumes:
      - /sys/fs/cgroup:/sys/fs/cgroup:rw
    cgroupns_mode: host
    published_ports:
      - "9000:9000/tcp"
    networks:
      - name: sonarqube-test-net

provisioner:
  name: ansible
  inventory:
    host_vars:
      sonarqube-rocky9:
        ansible_user: root

verifier:
  name: testinfra

scenario:
  name: default
  test_sequence:
    - dependency
    - destroy
    - syntax
    - create
    - prepare
    - converge
    - idempotence
    - verify
    - destroy