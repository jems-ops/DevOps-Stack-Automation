---
############################################################
# STIG Control: V-205224
# SRG ID: SRG-APP-000504-DNS-000074
# Severity: CAT II
# Status: NOT A FINDING (Systemd/Journald Logging Enabled)
#
# Title:
# The DNS implementation must generate audit records for the
# success and failure of start and stop of the name server
# service or daemon.
#
# Enforcement Strategy:
# - Ensure systemd journald is active (logging backend)
# - Ensure named service is managed by systemd
# - Do NOT duplicate validation logic (no log parsing here)
#
# Comments:
# FreeIPA DNS relies on systemd and journald for audit logging.
# Lifecycle events (start/stop/restart) are automatically logged.
#
# Enforcement ensures:
# - journald service is active
# - named service is properly managed by systemd
############################################################

- name: "V-205224 | Informational Summary"
  debug:
    msg: |
      STIG V-205224 Enforcement:

      systemd-journald is enabled and running.
      named service is managed by systemd.

      Lifecycle audit logging (start/stop/restart)
      is handled automatically by journald.

      No additional enforcement required.
      Validation will confirm presence of lifecycle events
      in journald logs.
