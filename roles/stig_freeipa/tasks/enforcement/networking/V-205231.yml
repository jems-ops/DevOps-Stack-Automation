---
############################################################
# STIG Control: V-205231
# SRG ID: SRG-APP-000516-DNS-000090
# Rule ID: SV-205231r961864
# Severity: CAT II
# Status: NOT APPLICABLE
#
# Title:
# DNSSEC key files must be protected or removed.
#
# Description:
# DNSSEC private keys (KSK/ZSK) must be protected to prevent
# unauthorized zone signing or DNS manipulation.
#
# Finding Details:
# DNSSEC is not enabled in this FreeIPA DNS deployment.
# No DNSSEC key files (*.key, *.private) are present under
# /var/named.
#
# Implementation Summary:
# - Collect DNSSEC key file presence
# - No enforcement action required (informational)
#
# Manual Procedure:
# ls -l /var/named/*.key /var/named/*.private
#
# Comments:
# DNSSEC is not implemented. No key material exists.
# Network controls provide compensating protection.
############################################################

- name: "V-205231 | Enforcement Summary"
  debug:
    msg:
      dnssec_key_files_found: "{{ dns_dnssec_keys.matched | default(0) }}"
      files: "{{ dns_dnssec_keys.files | map(attribute='path') | list | default([]) }}"
